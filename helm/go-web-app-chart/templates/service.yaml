apiVersion: v1
kind: Service
metadata:
  name: go-web-app
spec:
  selector:
    app: go-web-app
  ports:
    - protocol: TCP
      port: 80                   # Port exposed outside the cluster
      targetPort: 8080           # Port your Go app runs on inside the container
  type: NodePort